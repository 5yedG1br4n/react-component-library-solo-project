// import React from "react";

// export default function Badge({ children, bgColor, txtColor, pill }) {
//   const bgClass = bgColor ? `badge-${bgColor}` : "";
//   const txtClass = txtColor ? `badge-${txtColor}` : "";
//   const badgeType = pill ? `badge-${pill}` : "";

//   return (
//     <div className="">
//       <h2>{children}</h2>
//     </div>
//   );
// }
